@using DevilDaggersInfo.Core.Replay.Events
@using DevilDaggersInfo.Razor.ReplayEditor.Store.State
@using DevilDaggersInfo.Razor.ReplayEditor.Utils
@using Fluxor
@inject IState<ReplayBinaryState> ReplayBinaryState

<EventViewer Title="Hit" GridConfig="grid-cols-4">
	<Head>
		<span>Entity ID A</span>
		<span>Entity ID B</span>
		<span>User Data</span>
	</Head>
	<Body>
		<span>@MarkupUtils.EntityType(Event.EntityIdA, ReplayBinaryState.Value.ReplayBinary.EventsData.EntityTypes)</span>
		<span>@MarkupUtils.EntityType(Event.EntityIdB, ReplayBinaryState.Value.ReplayBinary.EventsData.EntityTypes)</span>
		<span>@Event.UserData</span>
	</Body>
</EventViewer>

@code
{
	[Parameter]
	[EditorRequired]
	public HitEvent Event { get; set; }
}
