@page "/leaderboard/player-settings"
@using DevilDaggersInfo.Web.Client.Components.Leaderboards
@using DevilDaggersInfo.Web.Client.Components.Leaderboards.PlayerSettings
@inject MainApiHttpClient Http

<Heading Title="Player Settings" />

@if (GetEntries == null)
{
	<Loading />
}
else
{
	<LeaderboardTableInfo />
	<LeaderboardTableSettings Entries="GetEntries" Players="Players" />
}
