@page
@model DevilDaggersWebsite.Pages.Admin.CustomLeaderboardCategories.IndexModel

@{
	ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
	<a asp-page="Create">Create New</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.CustomLeaderboardCategory[0].Name)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.CustomLeaderboardCategory[0].SortingPropertyName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.CustomLeaderboardCategory[0].Ascending)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.CustomLeaderboardCategory[0].LayoutPartialName)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model.CustomLeaderboardCategory)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Name)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.SortingPropertyName)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Ascending)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.LayoutPartialName)
				</td>
				<td>
					<a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
					<a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
					<a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>