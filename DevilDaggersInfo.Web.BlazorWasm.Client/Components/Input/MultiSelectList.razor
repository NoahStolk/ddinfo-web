@inherits InputBase<List<string>>

<div class="inline-block w-64 bg-gray-1 text-color-text cursor-pointer">
	<div>
		<div class="w-full" @onclick="e => _show = !_show"><span class="pl-2 text-lg select-none">@SelectedDisplayValue</span></div>
		@if (Values != null)
		{
			foreach (string value in Values)
			{
				<div class="grid grid-cols-8 bg-gray-1">
					<span class="pl-2 text-lg col-span-7">@value</span>
					@{
						MarkupString str = CurrentValue?.Contains(value) == true ? new("✕") : new("✓");
					}
					<span class="w-8 cursor-pointer text-lg rounded block text-center" @onclick="e => Toggle(value)">@str</span>
				</div>
			}
		}
	</div>
</div>
