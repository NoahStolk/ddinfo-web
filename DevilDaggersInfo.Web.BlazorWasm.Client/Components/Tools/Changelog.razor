@using DevilDaggersInfo.Web.BlazorWasm.Shared.Dto.Public.Tools

<Heading Level="2">Changelog</Heading>

@foreach (GetToolVersion v in Versions)
{
	<Heading Level="3">@v.VersionNumber - @v.ReleaseDate.ToString(FormatUtils.DateFormat)@(v.ReleaseDate > FeatureConstants.TrackingToolDownloadCounts ? $" ({v.DownloadCount} downloads)" : string.Empty)</Heading>
	@foreach (GetToolVersionChange c in v.Changes)
	{
		<Change GetChange="c" />
	}
}

@code
{
	[Parameter, EditorRequired] public IReadOnlyList<GetToolVersion> Versions { get; set; } = null!;
}
