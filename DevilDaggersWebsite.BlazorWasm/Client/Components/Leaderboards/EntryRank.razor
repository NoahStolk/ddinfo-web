<div class="text-right">
	@if (PlayerId != 0)
	{
		<Tooltip Text="@($"ID: {PlayerId} (click to copy)")"><span @onclick="CopyToClipboard">@Rank.ToString(FormatUtils.LeaderboardIntFormat)</span></Tooltip>
	}
	else
	{
		<span style="color: #ff0;">@Rank.ToString(FormatUtils.LeaderboardIntFormat)</span>
	}
</div>

@code
{
	[Parameter] public int PlayerId { get; set; }
	[Parameter] public int Rank { get; set; }

	private async Task CopyToClipboard()
	{
	}
}
